include ':server', ':common'
include 'desktop', 'android', 'core'

sourceControl {
    gitRepository("https://github.com/EsotericSoftware/kryonet.git") {
        producesModule("kryonet:kryonet")
    }
}